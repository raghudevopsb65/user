appName: user
service:
  port: 8080
  type: ClusterIP
ingress:
  enabled: false
  scheme: null

configMap:
  enabled: true
  data:
    DOCUMENTDB: "true"
    MONGO_URL: 
    REDIS_HOST: roboshop-prod-elasticache-cluster.ppugfs.0001.use1.cache.amazonaws.com

externalSecret:
  enabled: true
